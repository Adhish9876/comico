# 🚀 QUICK IP CHANGE GUIDE - 5 Minutes

## All Places Where IP Appears (For Chat & Video Calls)

### ✅ Step-by-Step Instructions

#### **Step 1: Update the .env File (MAIN)**
```
File: .env
Line: 2
Change FROM:  SERVER_IP=172.20.10.9
Change TO:    SERVER_IP=YOUR_NEW_IP
```

#### **Step 2: (Optional) Update Python Fallback IPs**
```
File: client.py
Line: 22
Change: os.getenv('SERVER_IP', '172.20.10.9')  →  '172.20.10.9' to 'YOUR_NEW_IP'

File: video_module.py
Line: 25
Change: os.getenv('SERVER_IP', '172.20.10.9')  →  '172.20.10.9' to 'YOUR_NEW_IP'
```

#### **Step 3: (Recommended) Update HTML Form Default**
```
File: web/index.html
Line: 206
Change FROM:  value="172.20.10.9"
Change TO:    value="YOUR_NEW_IP"
```

#### **Step 4: (If Using HTTPS) Regenerate SSL Certificates**
```powershell
# Run in PowerShell (from project root)
mkcert -cert-file cert.pem -key-file key.pem localhost 127.0.0.1 YOUR_NEW_IP 0.0.0.0
```

#### **Step 5: Restart Services**
```
1. Stop all running services
2. Start server: python server.py
3. Start client: python client.py
   OR run: .\ShadowNexusClient.bat
```

---

## 📍 Complete Location Reference

```
PRIMARY SOURCE:
├── .env (line 2)
│
FALLBACK IPS (if .env missing):
├── client.py (line 22)
├── video_module.py (line 25)
│
UI CONVENIENCE:
├── web/index.html (line 206)
│
CERTIFICATES (if HTTPS):
├── cert.pem
├── key.pem
│
AUTO-UPDATED (no changes needed):
├── templates/video_room.html
├── templates/audio_room.html
├── web/app.js
└── server.py
```

---

## 🎯 Minimum Required Changes

**Just change 1 file:**
- Edit `.env` → `SERVER_IP=YOUR_NEW_IP`

**Then restart services and you're done!**

---

## 📋 All Occurrences

| File | Location | Type | Current IP |
|------|----------|------|------------|
| .env | Line 2 | Config | 172.20.10.9 |
| client.py | Line 22 | Python fallback | 172.20.10.9 |
| video_module.py | Line 25 | Python fallback | 172.20.10.9 |
| web/index.html | Line 206 | HTML value attr | 172.20.10.9 |
| cert.pem | All lines | SSL certificate | IP in CN/SAN |
| key.pem | All lines | SSL private key | IP in cert |

---

## ✨ What Gets Updated Automatically

- `templates/video_room.html` ← Gets IP from video_module.py
- `templates/audio_room.html` ← Gets IP from video_module.py  
- Browser client ← Gets IP from user's form input
- `server.py` ← Listens on 0.0.0.0 (all interfaces)

---

## 🧪 Quick Test

After changes, verify by:
```powershell
ping YOUR_NEW_IP
telnet YOUR_NEW_IP 5555
```

Then start client and try connecting!

---

## 📝 Copy-Paste Quick Reference

**Your new IP:** ___________________

**Find & Replace ALL occurrences of:**
- Old IP: `172.20.10.9`
- New IP: `YOUR_NEW_IP`

**In these files:**
1. `.env` (critical)
2. `client.py` (recommended)
3. `video_module.py` (recommended)
4. `web/index.html` (recommended)

